# VCS
.git
**/.git

# Dependencies + build output — rebuilt inside the image
node_modules
**/node_modules
dist
**/dist
coverage
**/coverage
.yarn/cache
**/.yarn/cache
.yarn/install-state.gz

# Runtime data — must never be baked into the image
data
mushpi-data
data-test
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Secrets
.env
.env.prod
.env.local
**/.env
**/.env.prod
**/.env.local

# Build tooling not needed in the context
Dockerfile
docker-compose.yml
docker-compose*.yml
.dockerignore
README.md

# Sub-repos that are never build inputs (keep the context lean).
# mushpi-grow / mushpi-mock are cloned here only for a local
# scripts/verify-release.sh run; mushpi-docs is never built at all.
# NOTE: mushpi-server and mushpi-client are the build inputs — the Dockerfile
# COPYs from both — and must NEVER be listed in this file.
mushpi-grow
mushpi-mock
mushpi-docs

# IDE / hooks
.vscode
.idea
.husky
